# Marketplace module

The public route is excluded from navigation and returns 404 while `MARKETPLACE_ENABLED=false`. Database tables for products, orders, items, coupons, payments and invoices are present. No payment adapter is intentionally connected in v1.
