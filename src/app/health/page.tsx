import { Activity, CheckCircle2 } from "lucide-react";
import { serviceStatus } from "@/lib/env";

export default function HealthPage() { const checks = [["تطبيق Next.js", true], ["تهيئة المصادقة", serviceStatus.auth], ["تهيئة قاعدة البيانات", serviceStatus.supabase], ["مزود إزالة الخلفية", serviceStatus.backgroundRemoval]] as const; return <div className="container-shell flex min-h-[60vh] items-center justify-center py-16"><div className="glass-panel w-full max-w-xl rounded-[28px] p-8"><div className="flex items-center gap-3"><span className="flex h-12 w-12 items-center justify-center rounded-2xl bg-[color-mix(in_srgb,var(--success)_9%,transparent)] text-[var(--success)]"><Activity className="h-6 w-6" /></span><div><p className="text-xs text-[var(--muted)]">Health Check</p><h1 className="font-display mt-1 text-xl font-bold">حالة النظام</h1></div></div><div className="mt-7 space-y-3">{checks.map(([label, ready]) => <div key={label} className="flex items-center justify-between rounded-xl border border-[var(--line)] p-4 text-sm"><span>{label}</span>{ready ? <span className="inline-flex items-center gap-1.5 text-xs text-[var(--success)]"><CheckCircle2 className="h-4 w-4" /> جاهز</span> : <span className="text-xs text-[var(--warning)]">غير مهيأ</span>}</div>)}</div><p className="mt-5 text-xs leading-6 text-[var(--faint)]">عدم تهيئة خدمة اختيارية لا يعني تعطل المنصة. للتكامل الآلي استخدم <code>/api/health</code>.</p></div></div>; }
