export function LegalPage({ title, updated, intro, sections }: { title: string; updated: string; intro: string; sections: { title: string; paragraphs: string[]; items?: string[] }[] }) {
  return <div className="container-shell py-14 sm:py-20"><div className="mx-auto max-w-3xl"><p className="text-xs font-bold tracking-[.16em] text-[var(--brand)]">وثيقة أولية قابلة للتحديث</p><h1 className="font-display mt-4 text-3xl font-bold sm:text-4xl">{title}</h1><p className="mt-3 text-xs text-[var(--faint)]">آخر تحديث: {updated}</p><p className="mt-7 rounded-2xl border border-[var(--line)] bg-[var(--card)] p-5 leading-8 text-[var(--muted)]">{intro}</p><div className="mt-10 space-y-9">{sections.map((section) => <section key={section.title}><h2 className="font-display text-xl font-bold">{section.title}</h2>{section.paragraphs.map((paragraph) => <p key={paragraph} className="mt-3 leading-8 text-[var(--muted)]">{paragraph}</p>)}{section.items && <ul className="mt-4 space-y-2 pr-5 text-sm leading-7 text-[var(--muted)]">{section.items.map((item) => <li key={item} className="list-disc">{item}</li>)}</ul>}</section>)}</div></div></div>;
}
